#ifndef PJ_RECORD_HPP
#define PJ_RECORD_HPP

/* TODO: test this and make sure it works,
 * mostly has to do with the code generated
 * ---
 * how should i test this? bens paper?
 * ---
 * this should probably just be a struct...
 * or will that cause problems?
 * ---
 * this won't really be needed, unless we need
 * to have an internal representation
 * for some reason. i'll leave it for now
 * but technically you could just generate a struct
 * from the already-declared-and-typed-out record
 */

namespace pj_runtime
{
	struct pj_record
	{
		/* only needed as a uniform parent? */
	};
}

#endif
